apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: fitquest
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: auth-service
