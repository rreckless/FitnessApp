apiVersion: v1
kind: Service
metadata:
  name: workout-service
  namespace: fitquest
  labels:
    app: workout-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: workout-service
