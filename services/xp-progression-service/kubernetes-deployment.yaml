apiVersion: apps/v1
kind: Deployment
metadata:
  name: xp-progression-service
  namespace: fitquest
  labels:
    app: xp-progression-service
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: xp-progression-service
  template:
    metadata:
      labels:
        app: xp-progression-service
        version: v1
    spec:
      containers:
      - name: xp-progression-service
        image: xp-progression-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: xp-progression-secrets
              key: database-connection-string
        - name: ConnectionStrings__RabbitMQ
          valueFrom:
            secretKeyRef:
              name: xp-progression-secrets
              key: rabbitmq-connection-string
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
      securityContext:
        fsGroup: 1000
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - xp-progression-service
              topologyKey: kubernetes.io/hostname
