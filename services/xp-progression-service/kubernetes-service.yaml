apiVersion: v1
kind: Service
metadata:
  name: xp-progression-service
  namespace: fitquest
  labels:
    app: xp-progression-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: xp-progression-service
